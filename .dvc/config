[core]
    remote = minio-storage
['remote "minio-storage"']
    url = s3://dvc-storage
    endpointurl = http://localhost:9000
    ssl_verify = false
    access_key_id = admin
    secret_access_key = password123
    region = us-east-1
